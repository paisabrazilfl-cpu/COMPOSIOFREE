import { ComposioClient } from './client.js';

const client = new ComposioClient();

async function main() {
  console.log('ComposioFREE — free open-source integration layer for AI apps');
}

main();
